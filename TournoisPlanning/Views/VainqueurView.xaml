<UserControl x:Class="TournoisPlanning.Views.VainqueurView"  
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"  
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"  
            Height="Auto" Width="Auto">  
   <Grid Background="#E8F5E9" Margin="20"> <!-- Replaced 'Padding' with 'Margin' as Grid does not support Padding -->  
       <Border Background="White" CornerRadius="10" Padding="20" BorderBrush="#388E3C" BorderThickness="2">  
           <StackPanel HorizontalAlignment="Center">  
               <TextBlock Text="Vainqueur du Tournoi" FontSize="20" FontWeight="Bold" Foreground="#2E7D32" TextAlignment="Center" Margin="0,0,0,10"/>  
               <TextBlock Text="{Binding Vainqueur}" FontSize="28" FontWeight="SemiBold" Foreground="#1B5E20" TextAlignment="Center"/>  
           </StackPanel>  
       </Border>  
   </Grid>  
</UserControl>
